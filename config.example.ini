# EPWin Migrator - Configuração MySQL
# Copie para config.ini e ajuste os valores

# Configuração padrão para Docker (docker compose up -d mysql)
[mysql]
host = localhost
port = 3306
user = root
password = epwin
database = epwin
